Synthetic DMA / filelist fixtures only. No ROM-derived bytes.

`synthetic.csv` is three extractor-format lines (`offset,size,name,compressed,extract`)
used by `tools/det/test_rom_dma`. Public CI must not replace this with a
retail `filelist` extract or a dump image.
