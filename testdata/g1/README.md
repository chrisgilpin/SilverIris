G1 first-picture fixtures. CI stores a SHA-256 of the **greyscale**
framebuffer from a synthetic display list (fill + one shaded triangle).

Do not commit screenshots or ROM-derived frames. Title / Facility skybox
pictures come later, once stage load runs; their hashes stay private.
