G0 fixtures. The on-disk record can contain assets when taken from a real
title task — **never commit those**. Public CI stores a SHA-256 of the
canonical Gfx words of a synthetic display list (`synthetic.words.sha256`).

`tools/gbi_dump.py` reads G0T1 and prints the opcode histogram plus that hash.
